pipeline {
    agent { label (JAVA_17) }
    triggers { pollSCM (* * * * *) }
    stages {
        stage(VCS) {
            steps {
                git url: 'https://github.com/avsrpavankalyan/spring-petclinic.git',
                git branch: 'build'
            }
        }
    }
}